export function pairingReply(endpoint, ownerId, accountId, key) {
  if (!/^https:\/\/[a-z0-9.-]+$/i.test(endpoint || '')) throw new Error('PUBLIC_ENDPOINT must be configured');
  return `Private pairing for Roblox ${accountId}. Run this ONCE on that account. It saves its key locally and connects immediately; never share this message. Your current character-card summaries sync privately to your Discord group.\n\n\`\`\`lua\ngetgenv().CLAW_PAIR = {Endpoint="${endpoint}", OwnerId="${ownerId}", AccountId="${accountId}", Key="${key}"}\nloadstring(game:HttpGet("https://raw.githubusercontent.com/Clawdews/CLAW/control-beta/dist/launcher-beta.lua"))()\n\`\`\`\nAfterward use that same public loadstring in autoexec, without the private CLAW_PAIR line. /claw setup has it.`;
}
export const setupReply = 'CLAW setup\n\n**Several accounts on one device**\n1. Put this public loader in autoexec once:\n```lua\nloadstring(game:HttpGet("https://raw.githubusercontent.com/Clawdews/CLAW/control-beta/dist/launcher-beta.lua"))()\n```\n2. Open `/claw panel` → **Setup** → **Start alt setup**.\n3. Run the private snippet Discord gives you once in that executor workspace. Do not paste it into every account.\n4. Open each unpaired Roblox account and let autoexec run. Click **Refresh**, match its 8-character console code, then approve it.\n\n**One account only**\nUse `/claw enroll account:<Roblox username>`, then run its private reply on that account.\n\n**Finish**\nLeave each alt at character selection, approve one character in the main’s region, select the main, then enable following. Pairing and character approval are separate safety steps.\n\nUse Roblox usernames, not display names. No bot token or Roblox cookie is needed. Every Discord user has a separate account group.';
