# Copy to .env locally, fill in privately, and never commit the filled file.
# The bot token is only used by register.mjs, not by the running Worker.
DISCORD_BOT_TOKEN=
DISCORD_APPLICATION_ID=
DISCORD_GUILD_ID=
