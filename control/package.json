{
  "name": "claw-control",
  "private": true,
  "version": "0.2.0-beta.3",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run",
    "check:shared": "wrangler deploy --config wrangler.shared.jsonc --dry-run",
    "build:client": "node ../tools/build-control.mjs",
    "deploy": "wrangler deploy",
    "register": "node register.mjs"
  },
  "devDependencies": { "wrangler": "4.129.0", "miniflare": "5.20260903.0-alpha" }
}
